- 环境设置
```bash
export MUJOCO_GL=egl
export MESA_D3D12_DEFAULT_ADAPTER_NAME=NVIDIA
```
- 命令行：
	- `python run_sb3_ppo.py --env_name h1hand-door-v0 --seed 42 --load_pretrained --exp_name`

---

- 增加 MLP 大小
- `ppo_h1hand-door-v0_seed42` 出现 reward hacking 现象
	- 增加 max_step 500 → 1000
	- 增加 curriculum
- `curriculum`  有好转，但仍然小碎步倾斜身体，抖动严重
	- 考虑使用官方给的 `reach_one_hand` 开始训练
- `from_reach_one_hand` 已废弃，obs 维度不匹配（55 vs 155），失败
	- 调整 curriculum 一阶段权重 3:7 → 4:6，二阶段权重 2:3:5 → 3:3:4，更看重站稳
	- 调整二阶段阈值为 650 → 700
- `curriculum-v1` 已废弃，即使提高了 stand 的比重，机器人依然很难做出正常站姿
	- 考虑回到 `from_reach_one_hand`，使用 `splice_pretrained_into_model` 来把机器人本体感受部分添加到新的模型中
	- 奖励权重回退
- `from_reach_one_hand` 没能解决“帕金森”动作
	- 增加 `ActionSmoothnessWrapper` 和 `ControlCostWrapper`
	- 调整阈值 700 → 500，900 → 750
- `smooth_action` 陷入二阶段局部最优，怼门上不开门
	- 保留 `splice_pretrained_into_model` 和 max_step，其余回归原始看看情况
	- 调整学习率：1e-4 → 3e-5
- `base` 失败，表现和 `ppo_h1hand-door-v0_seed42` 类似，反手按门把
	- 重构 curriculum 试试
- & `curriculum-v2` 似乎还行
	- 在 stage2，机器人选择把门把手按到最死，一直按着，但是没有任何试图开门的举动，也没有尝试后退；从训练期的 `curriculum/avg_door_openness` 曲线来看，前期还是有 0.005 了，后期逐渐降低到 0，怀疑是为了按门把手而将体重压在了门板上
	- 增加 `body_door_penalty` 惩罚冲向门的速度
- `curriculum-v3`
	- 机器人用手臂勾住门把手按下，左脚伸直，右脚重心抵着门板，身体呈现和门差不多 30 度夹角，看起来重心是放在左脚上的，但保持静态（这个姿势居然没能拉开门？？）
	- 增加 `backward_reward`，从 stage3 开始继续训练
	- `curriculum-v3.1` 它似乎在努力了，但是门纹丝不动，想不通
		- 尝试把 assets 里 door_hinge 调小：50 → 10，看看能不能开门
	- `curriculum-v3.2` 失败，右脚离地也拉不动门
		- 考虑是因为 simulation 环境不够严谨？虽然机器人像是受力平衡，但力没有给到门？
		- 增加奖励手在 hatch 与门板之间 `hand_hooking`
- `curriculum-v4`：已失效
	- 保存了一份进入 stage2 的模型参数
	- 突然想起来判断 stage2 → stage3 不能用 hooking rate，要不然把手按下去了 rate 怎么都不可能达标
	- 把 hooking bonus 和对应的转阶段条件挪到 stage1
- `curriculum-v5`：能够稳定 hook 和 hatch，尚且不能开门
	- 保存了一份 stage1 的模型参数（稳定站稳+hook），从这里开始训练
	- 因为转阶段条件似乎难以达到所以手动转阶段
	- `curriculum-v5.2`：进入 stage3
		- 保存了一份稳定 hook 和 hatch 的参数
		- 把原来给身体/手的-x 方向速度奖励，调换成直接奖励 door_openness 的增量
- `curriculum-v6`：能够反复拉开门（0.1），训练因内存原因中断
	- 修改 reward，奖励速度改回奖励位置
	- ……（因为不知名原因在 wandb encoding video 的时候多次中断，关掉 gif 生成）
	- ~~`curriculum-v6.2`：开门程度 0.4，作为后续训练基础~~
	- `curriculum-v6.3`：在 stage4 很久但没有穿过门，开门程度很高，作为后续训练基础![[Pasted image 20260610215548.png]]![[Pasted image 20260610215601.png]]
	- `curriculum-v6.4`：说明从 `curriculum-v6.3` 开始训练会在一定步数内遗忘 hook 和 hatch 的技能 ![[Pasted image 20260610215629.png]]
		- 让 stage4 保持 stage3 的奖励，结果与 6.4 一致，说明是 PPO 更新把 checkpoint 毁掉了
		- 可能是数据加载的问题
			- 修正 `model.set_env(env)` 的顺序：虽然没有快速降到 0，但是在靠近 0 的地方波动![[Pasted image 20260610222944.png]]
			- `env.training=False`，冻结 VecNormalize：结果同上
			- 使用纯推理脚本 `visualize.py`：推理正常无误
		- 可能是更新幅度的问题
			- `reset_num_timesteps=False`：没有效果
			- 降低 learning rate 3e-5 → 1e-5：也没有效果

> [!bug] 最终问题
> ```python
> # env = VecNormalize.load("models/h1hand-door-v0/curriculum-v6.3/best_vecnormalize.pkl", env)
> 
> env = VecNormalize.load("models/h1hand-door-v0/curriculum-v6.3/best_vecnormalize.pkl", env.venv)
> ```
> 并修正 `model.set_env` 位置
> 
> 在 Python 中，`model.set_env(env)` 传递的是对象的**引用**。当你随后将局部变量 `env` 重新赋值为第 2 层 `VecNormalize` 时，**`model` 内部绑定的环境依然是第 1 层 `VecNormalize`**。
> 
> - 结果就是：模型一直在用**没有加载预训练参数**的第 1 层环境进行训练。
> - 因为第 1 层环境和 `eval_env` 都是**单层包装**，所以 `sync_envs_normalization` 遍历时没有发现层数不匹配，自然就不会报错。
> 
> **修改后的代码**不仅解决了报错，还**修复了原代码的 Bug**，确保模型真正加载并使用了 `best_vecnormalize.pkl` 中的预训练归一化参数。

> [!warning] 声明
> 因为在 stage4 之前完全没有发现以上 bug，而 `curriculum-v6.3` 是基于 `curriculum-v6.1` 的，而 `curriculum-v6.1` 应该也是继承之前的模型参数，所以整个训练过程很大概率无法复现

- `curriculum-v6.5`：增加抬腿奖励鼓励行走，但是模型试图通过踏步方式来获取这个奖励，并逐渐遗忘需要开门

- `curriculum-v7`：通过惩罚后退，使成功率实现 0 的突破（？），需要进一步 eval；但是成功的标准定的有点问题，从头再来  → 修改成功标准后成功率归 0 了 orz
	- `curriculum-v7.1`：改进惩罚机制，适当惩罚+奖励 → best 成功率不为 0，但 latest 退化严重
	- `curriculum-v7.2`：改进惩罚机制，能够让机器人有 3%的成功率

```python title:7.0
elif self.stage == 4:
	# 基础项不变
	custom_reward = 0.1 * s_r - 0.05 * (1 - ctrl) + step_penalty
	custom_reward += 0.3 * (5.0 * open_r + 0.1 * hatch_r)
	
	door_openness = metrics["door_openness"]
	hatch_angle = metrics["hatch_angle"]
	robot_x = metrics["robot_x"]
	door_x = metrics["door_x"]
	
	# ===== 负向约束列表（优先级从高到低）=====
	
	# 约束1：没开门时，禁止远离门
	if door_openness < 0.2 and robot_x < door_x - 0.5:
		custom_reward -= 2.0
	
	# 约束2：门打开后，禁止在不前进
	if door_openness > 0.5 and hatch_angle > 0.75:  # 门已开
		if len(self.episode_robot_x) > 20:
			# 检查最近20步的前进进度
			recent_x = self.episode_robot_x[-20:]
			progress = max(recent_x) - min(recent_x)
			
			# 细分惩罚级别
			if progress < 0.02:  # 几乎完全不动
				custom_reward -= 3.0
			elif progress < 0.05:  # 前进缓慢
				custom_reward -= 1.0
			elif progress < 0.1:  # 有点慢但不惩罚
				pass  # 不惩罚
			# else: 正常前进，不惩罚
			
			# 可选：反向移动惩罚（后退）
			if recent_x[-1] < recent_x[0] - 0.05:
				custom_reward -= 2.0  # 整体后退
				
	# 唯一的大奖励：成功穿过
	if robot_x > door_x + 0.5 and not hasattr(self, 'passed_door'):
		custom_reward += 50.0  # 一次性高奖励
		self.passed_door = True
	
	custom_reward += 1.0 * pass_r
```

